apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: user-mtls
  namespace: k8s
spec:
  selector:
    matchLabels:
      app: user-service
  mtls:
    mode: STRICT