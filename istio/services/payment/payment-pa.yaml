apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: payment-mtls
  namespace: k8s
spec:
  selector:
    matchLabels:
      app: payment-service
  mtls:
    mode: STRICT